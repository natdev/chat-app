import { AppComparePassword } from './app-compare-password';

describe('AppComparePassword', () => {
  it('should create an instance', () => {
    const directive = new AppComparePassword();
    expect(directive).toBeTruthy();
  });
});
